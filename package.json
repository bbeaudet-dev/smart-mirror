{
  "name": "smart-mirror",
  "version": "1.0.0",
  "description": "Smart Mirror with AI Integration - Full Stack Application",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client-mirror && npm run dev\" \"cd client-mobile && npm run dev\"",
    "build": "cd client-mirror && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd client-mirror && npm install && cd client-mobile && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "smart-mirror",
    "ai",
    "openai",
    "react",
    "nodejs",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.12",
    "@vitejs/plugin-react": "^5.0.1",
    "concurrently": "^8.2.2",
    "vite": "^7.1.3"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}

# AI Smart Mirror - Client Environment Variables
# This file contains client-side configuration only

# Client Configuration (Vite)
VITE_API_URL=http://localhost:5005

# Local Network IP (for phone access)
# Set this to your computer's local IP address
# Find it with: ifconfig | grep "inet " | grep -v 127.0.0.1
LOCAL_IP=your_local_ip_here

# Note: Server configuration and API keys are in server/.env
# See server/env.example for server-specific configuration
